interface:
  display_name: "CN Patent Application Cluster"
  short_description: "Chinese patent drafting agent workflow"
  default_prompt: "Use $cn-patent-application-cluster to mine patent points, draft a CNIPA-style application, run review gates, and package the deliverables."
policy:
  allow_implicit_invocation: true
