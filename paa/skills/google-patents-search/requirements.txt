# 免费专利检索 / 核验 skill 的依赖。**不使用浏览器自动化，无需任何 API key。**
#
# 必需：curl_cffi —— 以 Chrome 的 TLS/HTTP2 指纹发请求。
#   实测普通 requests 与无头浏览器都会拿到 Google 的 "Sorry… automated queries" 页，
#   而 curl_cffi 在多数网络下可正常取数（本机出口被 Google 判定可疑时仍会 503，此时如实上报）。
curl_cffi>=0.5

# 可选：官方 Google Patents 数据集（BigQuery）通道。装了并且有 GCP 凭据时会自动可用。
#   pip install google-cloud-bigquery db-dtypes
#   （凭据：gcloud auth application-default login 或 GOOGLE_APPLICATION_CREDENTIALS）
# google-cloud-bigquery>=3.11
# db-dtypes>=1.2

# 可选：中继通道（tavily 后端）。当出口被 Google 503 拦下时，用它取同一公开页的全文，
# 或在 patents.google.com 域内检索；只需一个 API key，不需要 GCP。
#   设置 TAVILY_API_KEY（或写在当前目录/上层的 .env 里），无需额外 pip 依赖。
# 其余通道（google / patentscope / 核验）一律不需要任何 key。
