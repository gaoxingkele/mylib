# 设计绕行 Round 1 — v1 → v2：事实三元组 → 冲突标记

## 目标对比文件

**CN121659916A**（对话生成领域在先申请，2026-01 公开）——独权第3步："将结构化事实数据注入预设的约束性提示模板"。

## 机制前后

| | 机制_before (v1) | 机制_after (v2) |
|---|---|---|
| 提示词内容 | 结构化事实三元组（"X 是 Y 因为 Z"） | 冲突标记（"候选 A 与图谱关系 R 矛盾，请回避"） |
| **注入动作** | **是** | **是**（未变） |
| 数据流向 | 检索 → 事实 → 注入 → LLM | 检索 → 标记 → 注入 → LLM |

## 判定：仅"换宾语"，未换动词

CN121659916A 圈走的是"**把结构化内容注入提示模板**"这一动作本身。Round 1 把注入的内容从"事实三元组"换成"冲突标记"——属于换宾语。

**冲突标记这一新内容并不能脱离提示模板存在**：它仍以文本形式进入提示词，并仍依赖"提示词指令让模型回避"——这与对方"注入+生成后校验"是同一种"约束经由提示词传递"的机制。

## 回归验证（v1 → v2）

- 语义检索 P05-1 v2 独权 → CN121659916A 仍为 top1（语义分 0.875）
- 设计上看起来避开了，实测仍在射程内

## 结论：未构成机制级绕行

→ 见 logic/solution/constraints.md §机制级绕行决策
→ 见 trace/exploration_tree.yaml §DA-01
→ 见 round_3_injection_to_sealing.md（机制级绕行的实际落地）