# 说明书摘要

本发明公开一种基于知识图谱校验-约束闭环的检索增强生成防幻觉方法。方法包括：接收查询，并行执行关键词/向量/图谱多跳检索并融合；查询层按类型分类动态调整权重；检索层对候选做图谱关联路径匹配并对不匹配者施加惩罚因子降权；生成层对重排序后每条结果构建结构化封箱，按校验结果裁决为可用态或隔离态，仅可用态封箱装配进大语言模型输入，隔离态封箱不进入模型输入；元适配层生成结构化元策略对象驱动生成端门控。装置包括查询层增强、检索层校验、封箱裁决、元适配门控模块。本发明提高检索一致性、抑制生成幻觉，适用于 RAG 与知识图谱问答场景。

摘要附图：图1

字数：约 220 字（含标点）。