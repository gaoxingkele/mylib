# Archival template

This file was a Claude-era slash-command template for starting a Ralph loop.

Do not execute or port it verbatim. In Codex/OMX, use the native `$ralph` workflow instead. If you need to migrate behavior, extract only the reusable concepts:
- how the next task is selected
- what verification command proves completion
- what state or guardrail files should persist across iterations
