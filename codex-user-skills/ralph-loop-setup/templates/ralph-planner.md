# Archival template

This file was a Claude-era planner command for generating Ralph tasks from GitHub issues.

If you need similar behavior in Codex/OMX:
- fetch issues with the GitHub app or CLI
- write repo-local plan artifacts under `.omx/plans/` or the repo's preferred location
- use `update_plan` and native agent workflows instead of legacy command frontmatter
